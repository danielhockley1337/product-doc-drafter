List all subdirectories inside `projects/` (excluding README.md). For each one, check whether a `working-notes.md` file exists inside it.

Then say:

> Here are your existing projects:
>
> [list each project folder, with a note if working-notes.md exists: "- homepage-redesign (working notes available)"]
>
> Which one would you like to pick up?

When the PM names a project:

1. Read all files in that project folder, starting with `working-notes.md` if it exists.
2. Summarize where the project was left off: what document is in progress, what was last decided or drafted, and any open questions noted.
3. Ask: "Want to pick up where we left off, or is there something specific you want to work on?"
